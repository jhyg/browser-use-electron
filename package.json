{
  "name": "browser-use-agent",
  "version": "1.0.0",
  "description": "Browser-Use Agent - Background service for process-gpt.io",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . browser-use-agent --platform=win32 --arch=x64 --out=dist --overwrite",
    "build:win": "electron-packager . browser-use-agent --platform=win32 --arch=x64 --out=dist --overwrite",
    "build:mac": "electron-packager . browser-use-agent --platform=darwin --arch=x64 --out=dist --overwrite",
    "build:linux": "electron-packager . browser-use-agent --platform=linux --arch=x64 --out=dist --overwrite",
    "dist": "electron-packager . browser-use-agent --platform=win32 --arch=x64 --out=dist --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["browser-use", "agent", "automation", "process-gpt"],
  "author": "Process-GPT",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "electron": "^35.1.2",
    "electron-packager": "^17.1.1"
  }
}
